# Enterprise Configuration for Large Business Scale

## Connection Pooling (add to .env)
# Optimal for large business with high concurrent users
DATABASE_URL="postgresql://user:password@host/db?pgbouncer=true&connection_limit=100"
DIRECT_URL="postgresql://user:password@host/db"

# Connection pool settings
PRISMA_CLIENT_ENGINE_TYPE=binary
